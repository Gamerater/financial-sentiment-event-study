# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Downloaded model weights (FinBERT ~440MB, don't commit to git)
.cache/
*/models--*
~/.cache/huggingface/

# Large / regenerable data files (keep repo lightweight; data is regenerated by scripts)
data/raw/*.csv
data/processed/*.csv
!data/raw/.gitkeep
!data/processed/.gitkeep
# Keep the small synthetic demo dataset committed -- it's needed so the repo
# has a working, viewable demo result even before real data is collected.
!data/processed/event_study_dataset_DEMO_SYNTHETIC.csv

# OS junk
.DS_Store
Thumbs.db

# Jupyter checkpoints
.ipynb_checkpoints/

# Editor
.vscode/
.idea/
